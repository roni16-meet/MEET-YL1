class Animal:
	def __init__(self, name, color, age, size,):
		self.name= name
		self.color= color
		self.age= age
		self.size= size
	def print_all(self):

		print(self.name)
		print(self.color)
		print(self.age)
		print(self.size)
		print("roni gever")


alpaka = Animal(name="Sokrates", color="purple", age=6, size="2.5  cubic roni gever ")
print("roni gever")
lama= Animal(name="Nikolau", color="bezh" age= 17, size= "0.5 cubic roni gever"
alpaka.print_all()
